// E:\CodeLve\src\llm\mock_llm.cpp
#include "mock_llm.h"
#include "../utils/logger.h"

namespace codelve {
    namespace llm {
        // The implementation is already fully defined in the header,
        // so this file primarily exists to satisfy the build system.
        // You could add any additional non-template methods here if needed.

        // Example of a non-static method if you decide to add one later:
        /*
        void SomeNewMethod() {
            utils::Logger::staticLog(utils::LogLevel::INFO,
                "MockLlm: Additional functionality called");
        }
        */
    }
} // namespace codelve::llm